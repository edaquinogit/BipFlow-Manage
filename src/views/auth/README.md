Auth views and forms for password recovery and reset.
Supported URLs:
- /forgot-password
- /reset-password?token=...
- /reset-password/:token

Conventions:
- Forms live in components/
- Services live in services/
- Tests live in tests/
